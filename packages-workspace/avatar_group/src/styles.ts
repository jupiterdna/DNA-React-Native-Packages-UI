import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  avatarContainer: {
    flexDirection: "row",
    gap: -8,
  },
  avatarWrapper: {
    borderWidth: 2,
    borderColor: "white",
  },
});
