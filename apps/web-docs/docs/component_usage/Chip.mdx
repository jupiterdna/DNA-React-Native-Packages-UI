import './styles/style.css'

```SnackPlayer name=Chip&description=This%20is%20a%20description&dependencies=@rndna/chip,@rndna/icon,react-native-svg&platform=web
 import * as React from 'react';
 import {View, StyleSheet} from 'react-native'
 import { DNAChip } from '@rndna/chip';
 import { UserIcon } from '@rndna/icon';

 const YourApp = () => (
    <View style={styles.container}>
      <DNAChip
        label="Chip"
        isClosable
        isLoading
        icon={UserIcon}
        size="md"
        onPress={() => console.log('pressed')}
        onPressClose={() => console.log('closed')}
        />
    </View>
 );
 
 const styles = StyleSheet.create({
   container:{
     padding: 20
   }
 })

 export default YourApp;
 ```
```
